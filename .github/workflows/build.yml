name: Build Tagtune

on: [push, pull_request]

jobs:
  macos-intel-build:
    runs-on: macos-12

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: ./action_macos_intel.sh

      - name: Run
        run: ./tagtune
